# Requirements for building wheels and PEX files.
# These have a conflicting setuptools pin (needs <41 for PEX compatibility)
# so they are kept separate from pyproject.toml dependency groups.
pex==2.1.153
pip>=20.3.4
setuptools>=20.3,<41,!=34.*,!=35.*
beautifulsoup4==4.8.2
requests==2.27.1
pkginfo==1.8.2
wheel>=0.31.1
importlib-metadata==4.8.3
importlib_resources==5.4.0
