/* eslint-env mocha */
import Vue from 'vue-test'; // eslint-disable-line
import assert from 'assert';

describe('downloadChannelMetadata action', () => {});

describe('transferChannelContent action', () => {
  it('during LOCALIMPORT, makes correct request to startdiskcontentimport', () => {});

  it('during LOCALEXPORT, makes correct request to startdiskexport', () => {});

  it('during REMOTEIMPORT, makes correct request to startremotecontentimport', () => {});
});
