import { createTranslator } from 'kolibri.utils.i18n';

export default createTranslator('DemographicInfoStrings', {
  genderLabel: 'Gender',
  birthYearLabel: 'Birth year',
  preferNotToSayOption: 'Prefer not to say',
});
