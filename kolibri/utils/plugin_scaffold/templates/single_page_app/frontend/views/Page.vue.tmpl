<template>

  {% verbatim %}<div>{{ heading$() }}</div>{% endverbatim %}

</template>


<script>

  import { createTranslator } from 'kolibri/utils/i18n';

  const strings = createTranslator('{{ translator_namespace }}', {
    heading: {
      message: '{{ readable }}',
      context: 'Heading of the main page.',
    },
  });

  export default {
    name: '{{ pascal }}Page',
    setup() {
      const { heading$ } = strings;
      return { heading$ };
    },
  };

</script>
